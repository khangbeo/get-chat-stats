Python program to get the top chatter from streamelements API.
It puts the output in a text file called "top_chatter.txt"
Add a list of users to ignore with a text file "excluded_users.txt". Usually your bots or yourself.
You'll need a .env with USER and TOKEN variables to use the app
